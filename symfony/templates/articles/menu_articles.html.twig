<ul class="menu-user">
	<li>
		<a
			href="{{ path('userProfile', {username: article.user.username}) }}">
			<!--SI L'USUARI TE IMATGE DE PERFIL-->
			{% if article.user.imatge %}
				<img src="{{ asset("img/imatges_perfil/" ~ article.user.imatge) }}" alt="imatge de perfil" id="img-modif">
			{% else %}
				<i class="fa fa-user-circle"></i>
			{% endif %}
		</a>
	</li>
	<li>
		<a class="menu-link menu-username" href="{{ path('userProfile', {username: article.user.username})}}">{{article.user.username}}</a>
	</li>
	<li class="menu-link article-data">
		{{article.datapublicacio|date("d M Y")}}
	</li>

	{% if app.user and (app.user == article.user) %}
		<li>
			<a class="menu-link logout-link" href="{{ path('app_logout')}}">
				sortir
			</a>
		</li>
	{% endif %}
</ul>

<!--RESTA DE ITEMS DEL MENU - PAGINA INICI-->
<ul class="menu-bottom">

	{% if app.user and is_granted('ROLE_ADMIN') %}
		<li>
			<a href="{{ path('article_update', {slug:article.slug}) }}">editar article</a>
		</li>
	{% endif %}
	<li>
		<a class="menu-link active" href="{{ path('articles_categoria', {cat_name: article.categoria.nom})}}">in{{article.categoria.nom}}</a>
	</li>
	<li>
		<a class="menu-link" href="{{ path('homepage')}}#about-us">b-NERD toDEVELOPE</a>
	</li>
	<li>
		<a class="menu-link" href="{{ path('homepage')}}#hard-skills">ARTICLES</a>
	</li>

	<!-- BUSCADOR -->
	<li id="search">
		<a class="menu-link" href="{{ path('homepage')}}#contact">SEARCH</a>
		{# <i class="menu-icon fa fa-search"></i> #}
	</li>
	<li id="hamburgesa">
		<i class="menu-icon fa fa-bars"></i>
	</li>
	<li id="dark-mode">
		<a href=""><i class="menu-icon fa fa-toggle-off"></i></a>
	</li>

</ul>
