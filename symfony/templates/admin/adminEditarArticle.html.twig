{% extends 'base.html.twig' %}
{% block navbar %}
	{{ include('admin/menu_admin.html.twig') }}
{% endblock %}

{% block content %}

	<section>
		<h1 class="titulo">Editar Article</h1>

		{{ form_start(adminEditarArticle) }}
		<div class="form-group form-row">

			<div class="col-md-6">
				{{ form_label(adminEditarArticle.data_publicacio) }}
				{{ form_widget(adminEditarArticle.data_publicacio) }}
			</div>
			<div class="col-md-6">
				{{ form_label(adminEditarArticle.data_actualitzacio) }}
				{{ form_widget(adminEditarArticle.data_actualitzacio) }}
			</div>
		</div>
		<div class="form-group form-row">
			<div class="col-md-6">

				{{ form_label(adminEditarArticle.autor) }}
				{{ form_widget(adminEditarArticle.autor) }}
			</div>
			<div class="col-md-6">
				{{ form_label(adminEditarArticle.visible) }}
				{{ form_widget(adminEditarArticle.visible) }}
			</div>
		</div>
		<button type="submit" class="btn btn-block btn-outline-secondary" alt="Confirmar l'actualització de dades">guardar</button>


		{{ form_end(adminEditarArticle) }}
		<br/>
		<a class="btn btn-outline-secondary" href="{{ path('article_detall', {slug: article.slug}) }}" alt="Enllaç a la pàgina del article">Veure article</button>
		<a class="btn btn-outline-secondary" href="{{ path('adminArticles') }}"  alt="Tornar a l'administrador d'articles">Cancelar</button>

	</section>

{% endblock %}
